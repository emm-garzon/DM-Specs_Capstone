import React from "react";

const AboutGameOne = ({ gameCard }) => {
  return (
    <div>
      <img className="gameCardOne" src={gameCard} alt="game card" />
    </div>
  );
};

export default AboutGameOne;
